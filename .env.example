# Optional admin credentials for the /admin panel (local dev backend).
# If unset, the defaults in src/admin/AuthContext.tsx are used.
# Copy this file to .env.local and adjust as needed.
VITE_ADMIN_EMAIL="you@example.com"
VITE_ADMIN_PASSWORD="change-me"
